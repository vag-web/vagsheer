/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "photos.app.goo.gl"], // Add "photos.app.goo.gl" here
  },
};

export default nextConfig;
